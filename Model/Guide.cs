using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Guide
{
    public static string[] instructions = {
        "The R buttons are just shorthand for Reset. These buttons just reset the respective values to their defaults.",
        "Select between the character’s stats or weapon, and none for scenarios where no modifiers should be in the dice throw.",
        "Where various options can be found regarding the game.",
        "Where the character’s stats can be saved, includes proficiency value. The stats are the modifier values, not the stats themselves.",
        "When 2x D20 or D100 dice are thrown, the total results will automatically show the advantage and disadvantage values as 2 of these particular dice are only used for that scenario and never for being added together."
    };


}
